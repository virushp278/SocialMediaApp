const require_connect = require('../../../../proto/src/components/connect/protobuf/connect.cjs');
const require_buffer = require('../../buffer.cjs');

//#region src/components/connect/strategies/core/statusReporter.ts
var StatusReporter = class {
	interval;
	intervalMs = 0;
	constructor(accessor, logger) {
		this.accessor = accessor;
		this.logger = logger;
	}
	/**
	* Update the status reporting interval. Restarts the timer if the interval
	* changed or if it wasn't running yet. A value of 0 disables reporting.
	*/
	updateInterval(ms) {
		if (ms === this.intervalMs && (this.interval || ms === 0)) return;
		this.intervalMs = ms;
		this.stop();
		if (ms > 0) this.start();
	}
	/** Stop the status reporting timer. */
	stop() {
		clearInterval(this.interval);
		this.interval = void 0;
	}
	start() {
		if (this.interval) return;
		this.interval = setInterval(() => this.tick(), this.intervalMs);
	}
	tick() {
		const conn = this.accessor.activeConnection;
		if (!conn || conn.ws.readyState !== WebSocket.OPEN) return;
		const inFlightRequestIds = Object.keys(this.accessor.inProgressRequests.requestLeases);
		const statusPayload = require_connect.WorkerStatusData.encode(require_connect.WorkerStatusData.create({
			inFlightRequestIds,
			shutdownRequested: this.accessor.shutdownRequested
		})).finish();
		conn.ws.send(require_buffer.ensureUnsharedArrayBuffer(require_connect.ConnectMessage.encode(require_connect.ConnectMessage.create({
			kind: require_connect.GatewayMessageType.WORKER_STATUS,
			payload: statusPayload
		})).finish()));
		this.logger.debug({
			connectionId: conn.id,
			inFlightRequestCount: inFlightRequestIds.length,
			shutdownRequested: this.accessor.shutdownRequested
		}, "Worker status sent");
	}
};

//#endregion
exports.StatusReporter = StatusReporter;
//# sourceMappingURL=statusReporter.cjs.map